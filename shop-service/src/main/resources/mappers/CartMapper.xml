<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codetest.shop.mapper.CartMapper">

    <resultMap id="cartMap" type="com.codetest.shop.entity.CartItem">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="cartMap">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="cartMap">
        SELECT * FROM cart WHERE user_id = #{userId}
    </select>

    <select id="selectByUserAndProduct" resultMap="cartMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, product_id, quantity, create_time)
        VALUES (#{userId}, #{productId}, #{quantity}, NOW())
    </insert>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

</mapper>
